[package]
name = "iced_palette"
version = "0.1.0"
edition = "2024"
description = "A command palette widget for Iced applications with fuzzy search"
license = "MIT"

[dependencies]
iced = { path = "../iced", features = ["advanced"] }
iced_widget = { path = "../iced/widget" }
